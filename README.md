# expense-tracker-app
1. Install NPM package in your terminal, command `npm-install`
2. Then enter command `npm start` in your terminal 
3. Then you can see app. In App you can add new expense and look list of your expenses